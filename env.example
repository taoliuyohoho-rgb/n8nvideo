# ==============================================
# 开发环境配置
# 复制此文件为 .env.local 并填写实际值
# ==============================================

# ============ 应用配置 ============
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
PORT=3000

# ============ 数据库配置 ============
# 开发环境使用 SQLite
DATABASE_URL="file:./dev.db"

# 生产环境使用 PostgreSQL（示例）
# DATABASE_URL="postgresql://username:password@hostname:5432/n8nvideo?schema=public"

# ============ 认证配置 ============
# 开发环境管理员账号（仅用于测试）
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD_HASH="$2a$10$example-hash-here"

# NextAuth 配置
NEXTAUTH_SECRET="development-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# ============ AI 服务配置 ============
# OpenAI
OPENAI_API_KEY="sk-proj-your-key-here"
OPENAI_ORG_ID=""  # 可选

# Google Gemini
GOOGLE_AI_API_KEY="your-gemini-api-key"

# 豆包 (Doubao)
DOUBAO_API_KEY="your-doubao-api-key"
DOUBAO_ENDPOINT="https://ark.cn-beijing.volces.com/api/v3"

# DeepSeek
DEEPSEEK_API_KEY="your-deepseek-api-key"
DEEPSEEK_BASE_URL="https://api.deepseek.com"

# ============ Google Cloud 配置 ============
# Google Sheets API
GOOGLE_SHEETS_ID="1q_ZqVw4DVRbcAA78ZVndXq4XcFEySNmRoLHiFkllFls"

# Service Account 文件路径（开发环境）
GOOGLE_APPLICATION_CREDENTIALS="./credentials/google-service-account.json"

# 或使用 JSON 字符串（生产环境）
# GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# Google OAuth 2.0 (可选)
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# ============ 云存储配置 (可选) ============
GOOGLE_CLOUD_PROJECT_ID="your-project-id"
GOOGLE_CLOUD_STORAGE_BUCKET="your-bucket-name"

# ============ 监控与日志 (可选) ============
LOG_LEVEL="debug"
SENTRY_DSN=""

# ============ Sora / Video Generation Provider ==========
# 通用视频生成配置（通过推荐引擎选中后作为兜底）
VIDEO_PROVIDER=""             # 例如: sora / doubao / veo（可留空，由估算引擎选择）
VIDEO_MODEL_NAME=""           # 例如: sora-1.0 / Doubao-Seedance-1.0-pro

# Sora (或兼容) 配置（如使用则填写）
SORA_API_KEY=""
SORA_BASE_URL=""
SORA_MODEL=""
